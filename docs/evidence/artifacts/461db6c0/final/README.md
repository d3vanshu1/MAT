# Final Artifacts — Run 461db6c0

## Status

No canonical finalization completed. No module_outputs row exists.

- `final/findings.json` → IDENTICAL to `postmerge/findings.json` (9 findings)
  The pipeline suppressed all 9 at the finding_reduction_gate; no further
  processing was applied (no absence verification, no report generation).

- `final/report.md` → DOES NOT EXIST.
  No report was generated by this pipeline run.
  - No tree_level=0 (merge root) checkpoint
  - No tree_level=98 (L3 export) checkpoint  
  - No tree_level=99 (diagnostic) checkpoint
  - No module_outputs row

## Source of "shipped report" (if one exists externally)

The only persisted finding data for this run is:
1. **191 L1 merge checkpoints** (tree_level=1, 6.5MB total) — individual chunk
   merge outputs before consolidation
2. **pipeline_checkpoints.finding_reduction_gate** — contains the consolidated
   9 findings in `suppressedLedger` array (persisted at 2026-08-13 03:17:43 UTC)

Any "rendered report" the user has seen for this run was generated externally
(e.g., via DiagnosticFinalization API, ExportL3RawFindings, or manual extraction
from the finding_reduction_gate checkpoint) — NOT by the pipeline's canonical
report generation stage.
